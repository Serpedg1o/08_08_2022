import React from 'react';
import { StyledWrapper } from './styled';


const StickerContainer = (props) => {
    return <StyledWrapper isTransperent={false}>StickerContainer</StyledWrapper>
};

export default StickerContainer;